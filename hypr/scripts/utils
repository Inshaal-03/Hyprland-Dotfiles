#!/usr/bin/env bash

utils=(Nmtui Bluetui Wiremix Btop Nvtop Yazi Clipse Disk-Usage)

util=$(echo "${utils[*]}" | rofi -dmenu -i -sep " " -p "Select which utility you want to open" -l "${#utils[*]}") # "${#utils[*]}" this gives us the length of an array

case "$util" in
  Nmtui)
    kitty --class nmtui -e nmtui
    ;;
  Bluetui)
    kitty --class bluetui -e bluetui
    ;;
  Wiremix)
    kitty --class wiremix -e wiremix
    ;;
  Btop)
    kitty --class btop -e btop
    ;;
  Nvtop)
    kitty --class nvtop -e nvtop
    ;;
  Yazi)
    kitty --class yazi -e yazi
    ;;
  Clipse)
    kitty --class clipse -e clipse
    ;;
  Disk-Usage)
    kitty --class gdu -e gdu
    ;;
  *)
    exit 0
esac
